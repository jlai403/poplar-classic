services:
  golf:
    image: ghcr.io/jlai403/poplar-classic:latest
    ports:
      - "3050:3000"
    volumes:
      - golf-data:/data
    environment:
      - DB_PATH=/data/golf.json
    restart: unless-stopped

volumes:
  golf-data:
